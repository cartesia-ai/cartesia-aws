[project]
name = "aws-workshop"
version = "0.1.0"
description = "Workshop: Cartesia Line voice agent on AWS Bedrock"
requires-python = ">=3.10"
dependencies = [
    "cartesia-line==0.2.10",
    "boto3>=1.34.0",
    "loguru>=0.7.0",
]

[tool.setuptools]
py-modules = ["main"]
